#!/bin/bash -e

# If running the rails server then migrate existing database
if [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; then
  ./bin/rails db:migrate

  # Starting Thinking Sphinx
  bundle exec rake ts:configure
  bundle exec rake ts:start
fi

exec "${@}"
