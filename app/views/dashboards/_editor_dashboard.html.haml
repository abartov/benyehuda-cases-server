!= render(:partial => "tasks/index", :locals => {:tasks => @editing_tasks.page(params[:editor_page]), param_name: :editor_page, :title => _("My Editing Tasks"), :editing => true, :total => current_user.editing_tasks.visible_in_my_tasks.length })

%h4= _("Other")
!= link_to((_("Unassigned tasks (%{count})") % {:count => Task.unassigned.count}), tasks_path)
