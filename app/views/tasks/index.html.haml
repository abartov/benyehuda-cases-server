.heading
  - set_tab :dashboard

.bar
  / add-nav
  %ul.add-nav
    %li
      %a.link1{ :href => dashboard_path }
        %span
          %em
            != _("back to dashboard")

!= render(:partial => "tasks/index", :locals => {:tasks => @tasks, :editing => false, :title => _("Unassigned Tasks")})
