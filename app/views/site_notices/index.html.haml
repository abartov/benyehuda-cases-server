= set_tab :site_notices, (params[:all] ? _("All Site Notices") : _("Active Site Notices"))

!= link_to I18n.t('site_notice.new'), new_site_notice_path
!= link_to I18n.t('site_notice.active'), site_notices_path
!= link_to I18n.t('site_notice.all'), site_notices_path(:all => true)

%table.list
  %tr.title
    %th= _("ID")
    %th= _("From")
    %th= _("To")
    %th= _("HTML")
    %th &nbsp;
    %th &nbsp;
  
  != render(:partial => collection)

!= will_paginate(collection)