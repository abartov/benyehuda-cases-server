<% if params[:add_team_id].present? %>
  <%# This response is now handled by the AJAX success callback in the form %>
<% else %>
  // Handle teams management view
  $('.tasks').html("<%= escape_javascript(render partial: 'teams/tasks', locals: { edit: true}) %>");
<% end %>