// TODO: move to ajax events for the different views calling this -- the below code is suitable for team management, but not for the individual task view, adding a team
$('.tasks').html("<%= escape_javascript(render partial: 'teams/tasks', locals: { edit: true}) %>");